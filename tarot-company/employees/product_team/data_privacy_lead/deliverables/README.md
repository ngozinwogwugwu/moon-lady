All data work must:

- Default to deletion unless retention is explicitly justified
- Model the archive as a lifecycle, not a store
- Treat user data with the same care as the ritual itself
- Version ontology changes so stored sessions remain interpretable in their original context

Posture: delete until forced to keep.
